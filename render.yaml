databases:
  - name: postgres
    ipAllowList: []
    databaseName: expense_tracker_RoR
    user: expense_tracker_RoR

services:
  - type: web
    name: my_app_render
    env: ruby
    buildCommand: './render-build.sh'
    startCommand: 'bundle exec rails s'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
